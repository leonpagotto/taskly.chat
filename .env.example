# Copy to .env.local in each app root or repo root if shared
OPENAI_API_KEY=
MODEL_CHAT=gpt-4o-mini
MODEL_FAST=gpt-4o-mini

# SQLite dev database for Prisma (Story 02)
DATABASE_URL="file:./dev.db"

# Model provider selection (currently supported: gemini)
MODEL_PROVIDER=gemini

# Gemini model provider (DO NOT COMMIT REAL KEYS)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-pro
