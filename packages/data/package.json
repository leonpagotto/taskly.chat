{
  "name": "@taskly/data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
  "test": "dotenv -e .env.test -- sh -c 'prisma db push --skip-generate && vitest run'"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5",
    "prisma": "^5.18.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.11.0"
  }
}
